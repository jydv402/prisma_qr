import 'package:flutter/material.dart';

class QuPage extends StatelessWidget {
  const QuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.black,
    );
  }
}
